# Architecture
Phase 1 establishes React/Vite frontend, Express REST backend, MongoDB models, authentication foundation, security middleware, AI abstraction placeholder, and Docker configuration. The target pipeline is query normalization -> extraction -> embeddings -> MongoDB Vector Search -> reranking -> relationship/version checks -> RAG explanation.
