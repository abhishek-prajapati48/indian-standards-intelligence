# Deployment
Recommended target: frontend on Vercel/Netlify or Docker, backend on Render/Railway/VPS/AWS, MongoDB Atlas, AI provider accessible only from backend. Configure environment variables in the hosting provider and never commit `.env`.
